#include<stdio.h>

void main()
{
    int i ;
    printf("Enter an integer:");
    scanf("%d",&i);
    if(i%2 == 0)
    printf("Number is divisible by 2");
    else
    printf("Number is not divisible by 2\n");
}
